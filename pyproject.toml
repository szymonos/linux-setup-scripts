[project]
name = "linux-setup-scripts"
version = "0.1.0"
requires-python = "~=3.12.0"
dependencies = []

[project.optional-dependencies]
docs = [
  "mkdocs-material",
  "mkdocs-mermaid2-plugin",
]

[tool.uv]
native-tls = true
